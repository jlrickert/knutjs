{
	"$schema": "https://json.schemastore.org/tsconfig",
	"extends": ["expo/tsconfig.base", "@jlrickert/knutjs-tsconfig/base.json"],
	"compilerOptions": {
		"jsx": "react-jsx",
		"module": "Preserve",
		"moduleResolution": "Bundler"
	},
	"include": ["./setup-jest.ts"],
	"exclude": [
		"metro.config.js" // Types are a disaster here. Ignoring because I can.
	]
}
